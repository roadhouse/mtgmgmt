<span>
  <i class="icon-addshape"></i> <%= deck.percent_owned(current_user, :main) %>%
  <i class="icon-subtractshape"></i> <%= deck.percent_owned(current_user, :sideboard) %>%
</span>

<%= link_to deck.archeptype_deck.deck, class: "pure-button" do %> 
  <%= deck.archeptype_deck.colors_list.join(" ").html_safe %>
  <%= deck.archeptype_deck.name %>
<% end %>
