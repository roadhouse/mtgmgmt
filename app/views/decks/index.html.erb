<h1>Listing decks</h1>

<table>
  <tr>
    <th>Name</th>
  </tr>

  <% @decks.each do |deck| %>
    <tr>
      <td><%= link_to deck.name, deck %></td>
    </tr>
  <% end %>
</table>

<%= paginate @decks %>
