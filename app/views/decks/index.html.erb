<h1>Listing decks</h1>

<table class="pure-table pure-table-horizontal">
  <tr>
    <th>Name</th>
  </tr>

  <% @decks.each do |deck| %>
    <tr>
      <td><%= link_to deck.name, deck %></td>
    </tr>
  <% end %>
</table>

<%= paginate @decks %>
